# rust-uuid-extra — Documentation

**Source:** `src/` — 6 Rust files. UUID generation, Base58/Base64 encoding, and timestamp extraction.

`uuid_extra` provides convenience wrappers around the `uuid` crate for generating UUID v4/v7, encoding them in Base58 or Base64 (standard, URL-safe, URL-safe-no-pad), and extracting epoch millisecond timestamps from v7 UUIDs.

## Documentation

- [Overview](00-overview.md) — API, UUID generation, Base58/Base64 encoding, timestamp extraction, error model
