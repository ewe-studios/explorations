# LangGraph.js -- TypeScript/JavaScript Port

TypeScript port of LangGraph — same Pregel algorithm, JS-native concurrency.

## Documents

- [00 Source Architecture](00-source.md) — Pregel runtime, channels, checkpointing, StateGraph, SDKs
