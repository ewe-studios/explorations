# Todo

You are to spawn exploration agents where each must singularly generate deep and detailed explorations of each of the project, create a directory. Once the exploration is done, spawn the rust agent to create the version of that type of project in rust.

**Important:**

1. Some explorations have already started before, were paused, review whats there, update what's necessary, continue where you left off. Remember do it one by one.
2. Do go duplicating code and copying files over, dont be stupid.
3. Run the agents on the list one by one, only multi-task within each item.
4. Nothing is completed, till you do a systematic review of each part of the directory or project to confirm completeness.
5. We are to do detailed exploration, not just one single exploration.md file, do deep dives into each sub project, sub-module, make it super detailed
6. Trigger up to 3 agents to parallelize the work on the items.

---

## Completed Explorations

- [x] turso - Comprehensive exploration with 15+ deep-dive documents in ./turso directory

---

## Pending Explorations

1. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.SpacetimeDB
  1. It should be created in a ./spacetimedb directory, including all the other projects in point 2 here above, create them all as sub-directory of ./spacetimedb and any sub-directory or related directory should exists under ./spacetimedb and no where else
  2. How its storage file format works, how do we recreate it, what are the trade offs and how does it stay performant.
  3. Review all directories within it and create a detailed exploration for each within ./spacetimedb

2. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.wasm/wasm-memory

3. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.wasm/src.fastcrdt

4. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.wasm/flatbuffers

5. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.wasm/src.InstantDB
  1. It should be created in a ./instantdb directory, including all the other projects in point 2 here above, create them all as sub-directory of ./instantdb and any sub-directory or related directory should exists under ./instantdb and no where else
  2. Review all directories within it and create a detailed exploration for each within ./instantdb

6. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.wasm/src.napi-rs
  1. It should be created in a ./napi-rs directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How to use this effectively in a rust project to interact with nodejs modules and others it supports

7. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.fframes
  1. It should be created in a ./fframes directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each

8. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.wasm/src.rustwasm
  1. It should be created in a ./rustwasm directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each

9. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.wasm/src.wasmtime
  1. It should be created in a ./wasmtime directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each

10. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.wasm/src.stdweb
  1. It should be created in a ./stdweb directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each

11. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.matrix-web
  1. It should be created in a ./matrix-web directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each

12. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.Makerpad
  1. It should be created in a ./makerpad directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each

13. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.lynxfamily
  1. It should be created in a ./lynxfamily directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each

14. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.lunatic
  1. It should be created in a ./lunatic directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each

15. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.jj-vcs
  1. It should be created in a ./jj-vcs directory, including all the other projects/directory within
  2. How it effectively stores file changes like git but in its own way, how it stores them, compresses them, does it uses patch or some other algorithm to effectively capture changes.
  3. Review all directories within it and create a detailed exploration for each

16. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.CodingIDE
  1. It should be created in a ./codingIDE directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. Does any of them already run on the web with wasm, how can we utilize this for our own usecase to have a web editor, can we bundle as a wasm file, how do we reproduce in rust in a production level way.

17. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.caddy
  1. It should be created in a ./caddy directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

18. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.Ash
  1. It should be created in a ./ash directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

19. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.dolthub
  1. It should be created in a ./dolthub directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

20. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.eyre
  1. It should be created in a ./eyre directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

21. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.facet-rs
  1. It should be created in a ./facet-rs directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

22. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.floneum
  1. It should be created in a ./floneum directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

23. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.inngest
  1. It should be created in a ./inngest directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

24. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.ironcalc
  1. It should be created in a ./ironcalc directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  4. How it utilizes wasm, instantiate in the web and non-web environment, what performance characteristics and choices do they make
  5. How can we copy its wasm approach for production level wasm applications on the web able to handle large data.

25. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.jamsocket
  1. It should be created in a ./jamsocket directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  4. How it utilizes wasm, instantiate in the web and non-web environment, what performance characteristics and choices do they make
  5. How can we copy its wasm approach for production level wasm applications on the web able to handle large data.

26. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.leptos
  1. It should be created in a ./leptos directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  4. How it utilizes wasm, instantiate in the web and non-web environment, what performance characteristics and choices do they make
  5. How can we copy its wasm approach for production level wasm applications on the web able to handle large data.

27. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.resonatehq
  1. It should be created in a ./resonatehq directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  4. How it utilizes wasm, instantiate in the web and non-web environment, what performance characteristics and choices do they make
  5. How can we copy its wasm approach for production level wasm applications on the web able to handle large data.

28. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.ratatui
  1. It should be created in a ./ratatui directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  4. How it utilizes wasm, instantiate in the web and non-web environment, what performance characteristics and choices do they make
  5. How can we copy its wasm approach for production level wasm applications on the web able to handle large data.

29. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.RPC
  1. It should be created in a ./rpc directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  4. How it utilizes wasm, instantiate in the web and non-web environment, what performance characteristics and choices do they make
  5. How can we copy its wasm approach for production level wasm applications on the web able to handle large data.

30. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.rubc-rust-inside-wasm
  1. It should be created in a ./rubc directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

31. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.RustSignals
  1. It should be created in a ./rustsignals directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

32. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.SpaceJam
  1. It should be created in a ./spacejam directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  
33. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.llamacpp/src.AICoders/src.VarcelLabs
  1. It should be created in a ./vercel_labs directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  
34. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.llamacpp/src.AICoders/src.VarcelLabs
  1. It should be created in a ./vercel_labs directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  
35. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.llamacpp/src.AICoders/src.BetterContext
  1. It should be created in a ./bettercontext directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
  
35. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.llamacpp/src.AICoders/src.BetterContext
  1. It should be created in a ./bettercontext directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.

36. Create an exploration for: /home/darkvoid/Boxxed/@formulas/src.rust/src.llamacpp/src.AICoders/src.MCP
  1. It should be created in a ./src.mcp directory, including all the other projects/directory within
  2. Review all directories within it and create a detailed exploration for each
  3. How do we reproduce its functionality in rust to a production level.
